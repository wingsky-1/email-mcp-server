# Email MCP 服务器测试计划

## 📋 测试现状分析

### 当前状态
- **测试目录**: tests/ 目录存在但为空
- **测试依赖**: 已安装完整测试工具链
- **代码覆盖率**: 0% （尚未开始）
- **目标覆盖率**: 90%+

### 测试工具链
```bash
✅ pytest>=9.0.1        # 核心测试框架
✅ pytest-asyncio>=1.3.0  # 异步测试支持
✅ pytest-cov>=7.0.0     # 覆盖率报告
✅ pytest-mock>=3.15.1   # Mock/Stub对象
✅ aiosmtpd>=1.4.6        # 测试SMTP服务器
```

## 🎯 测试目标

### 总体目标
1. **代码覆盖率**: 达到 90%+
2. **功能完整性**: 覆盖所有核心功能
3. **稳定性**: 确保生产环境可靠运行
4. **性能基准**: 建立性能基线

### 具体指标
- **单元测试覆盖率**: ≥ 90%
- **集成测试**: 覆盖所有主要工作流
- **性能测试**: 响应时间 < 30秒
- **内存使用**: 附件处理内存优化 15%

## 📊 测试分类和优先级

### 🔴 高优先级（必须完成）
#### 1. 单元测试（目标：7天）

##### 1.1 EmailService 测试（3天）
```python
# 测试文件: tests/test_email_service.py
- test_send_email_success()           # 正常发送邮件
- test_send_email_with_attachments()   # 带附件发送
- test_connection_management()        # 连接管理
- test_error_handling()                # 错误处理
- test_smtp_configuration()           # SMTP配置
- test_disconnection_cleanup()        # 断开清理
```

##### 1.2 AttachmentService 测试（2天）
```python
# 测试文件: tests/test_attachment_service.py
- test_process_local_attachment()      # 本地附件处理
- test_process_remote_attachment()     # 远程附件下载
- test_attachment_validation()         # 附件验证
- test_temp_file_cleanup()            # 临时文件清理
- test_file_size_limits()             # 文件大小限制
- test_download_retry_mechanism()     # 下载重试机制
```

##### 1.3 Pydantic 模型测试（1天）
```python
# 测试文件: tests/test_models.py
- test_send_email_tool_request()      # MCP工具请求模型
- test_email_validation_request()     # 邮箱验证请求
- test_attachment_model()             # 附件模型
- test_email_message_model()          # 邮件消息模型
- test_connection_info_model()        # 连接信息模型
- test_model_validation()             # 模型验证逻辑
```

##### 1.4 配置管理测试（1天）
```python
# 测试文件: tests/test_config.py
- test_email_settings()              # 邮箱配置
- test_app_settings()                # 应用配置
- test_environment_variables()        # 环境变量
- test_config_validation()           # 配置验证
- test_smtp_auto_detection()         # SMTP自动检测
```

### 🟡 中优先级（重要完成）
#### 2. 集成测试（目标：3天）

##### 2.1 端到端邮件发送测试（2天）
```python
# 测试文件: tests/test_integration.py
- test_complete_email_flow()         # 完整邮件发送流程
- test_email_with_html_content()      # HTML邮件
- test_email_multiple_recipients()    # 多收件人
- test_email_with_cc_bcc()           # 抄送/密送
- test_email_priority()              # 邮件优先级
```

##### 2.2 MCP工具集成测试（1天）
```python
# 测试文件: tests/test_mcp_integration.py
- test_send_email_tool()             # 发送邮件工具
- test_validate_email_tool()         # 邮箱验证工具
- test_check_config_tool()           # 配置检查工具
- test_get_providers_tool()          # 获取提供商工具
```

### 🟢 低优先级（增量完成）
#### 3. 性能测试（目标：2天）

##### 3.1 性能基准测试（1天）
```python
# 测试文件: tests/test_performance.py
- test_send_email_performance()      # 发送性能基准
- test_attachment_processing_speed() # 附件处理速度
- test_memory_usage()                # 内存使用测试
- test_concurrent_operations()       # 并发操作（如果支持）
```

##### 3.2 压力测试（1天）
```python
# 测试文件: tests/test_stress.py
- test_large_attachments()           # 大附件处理
- test_many_recipients()             # 大量收件人
- test_long_running_operations()     # 长时间运行
- test_resource_cleanup()            # 资源清理
```

## 🛠️ 测试基础设施

### 测试配置文件

#### pytest.ini
```ini
[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    --strict-config
    --cov=src/email_mcp_server
    --cov-report=html
    --cov-report=term-missing
    --cov-fail-under=90
markers =
    unit: 单元测试
    integration: 集成测试
    performance: 性能测试
    slow: 慢速测试
asyncio_mode = auto
```

#### conftest.py（测试配置）
```python
import pytest
import asyncio
from unittest.mock import Mock, patch
from email_mcp_server.config import get_email_settings, reload_settings

@pytest.fixture
def mock_email_settings():
    """模拟邮箱配置"""
    with patch('email_mcp_server.config.get_email_settings') as mock:
        mock.return_value = Mock(
            address="test@example.com",
            password="test_password",
            provider=Mock(value="gmail"),
            smtp_config=Mock(
                server="smtp.gmail.com",
                port=587,
                use_tls=True,
                use_ssl=False
            )
        )
        yield mock

@pytest.fixture
def temp_attachment_file(tmp_path):
    """临时附件文件"""
    import tempfile
    test_file = tmp_path / "test.txt"
    test_file.write_text("Test attachment content")
    return test_file

@pytest.fixture(scope="session")
def event_loop():
    """异步事件循环"""
    loop = asyncio.new_event_loop()
    yield loop
    loop.close()
```

### Mock SMTP 服务器
```python
# tests/fixtures/smtp_server.py
import asyncio
from aiosmtpd.controller import Controller
from aiosmtpd.smtp import SMTP as SMTPProtocol

class TestSMTPController(Controller):
    def __init__(self):
        super().__init__()
        self.messages = []

    async def handle_DATA(self, server, session, envelope, data):
        """处理邮件数据"""
        self.messages.append(data.decode('utf-8'))
        return '250 Message accepted'

@pytest.fixture
async def mock_smtp_server():
    """模拟SMTP服务器"""
    controller = TestSMTPController()
    smtp_server = await asyncio.start_server(
        controller, hostname='127.0.0.1', port=0
    )

    yield smtp_server

    smtp_server.close()
    await smtp_server.wait_closed()
```

## 📅 测试执行计划

### 第一周：单元测试（Day 1-7）

#### Day 1-2: EmailService 测试
```bash
# 执行命令
uv run pytest tests/test_email_service.py -v --cov=email_mcp_server.email_service

# 目标
- 覆盖率 ≥ 90%
- 所有测试通过
- Mock 外部依赖（SMTP连接）
```

#### Day 3-4: AttachmentService 测试
```bash
# 执行命令
uv run pytest tests/test_attachment_service.py -v --cov=email_mcp_server.attachment_service

# 目标
- 覆盖率 ≥ 90%
- 模拟网络请求
- 测试临时文件处理
```

#### Day 5: Pydantic 模型测试
```bash
# 执行命令
uv run pytest tests/test_models.py -v --cov=email_mcp_server.models

# 目标
- 覆盖率 ≥ 95%
- 测试所有验证逻辑
- 边界条件测试
```

#### Day 6: 配置管理测试
```bash
# 执行命令
uv run pytest tests/test_config.py -v --cov=email_mcp_server.config

# 目标
- 覆盖率 ≥ 90%
- 环境变量测试
- 配置验证测试
```

#### Day 7: 单元测试回顾和修复
```bash
# 执行命令
uv run pytest tests/ -m "unit" --cov=src/email_mcp_server --cov-report=html

# 目标
- 总覆盖率 ≥ 85%
- 所有失败测试修复
- 生成覆盖率报告
```

### 第二周：集成和性能测试（Day 8-14）

#### Day 8-9: 集成测试
```bash
# 执行命令
uv run pytest tests/test_integration.py -v -m "integration"

# 目标
- 端到端测试通过
- Mock SMTP 服务器正常工作
- 错误场景覆盖
```

#### Day 10-11: MCP 工具集成测试
```bash
# 执行命令
uv run pytest tests/test_mcp_integration.py -v -m "integration"

# 目标
- 所有 MCP 工具测试通过
- 参数验证测试
- 错误处理测试
```

#### Day 12: 性能基准测试
```bash
# 执行命令
uv run pytest tests/test_performance.py -v -m "performance"

# 目标
- 建立性能基线
- 响应时间记录
- 内存使用监控
```

#### Day 13: 压力测试
```bash
# 执行命令
uv run pytest tests/test_stress.py -v -m "performance"

# 目标
- 大附件处理测试
- 极限场景测试
- 资源清理验证
```

#### Day 14: 全面测试和报告
```bash
# 执行命令
uv run pytest tests/ --cov=src/email_mcp_server --cov-report=html --cov-report=term-missing

# 目标
- 总覆盖率 ≥ 90%
- 生成完整测试报告
- 性能基准文档
```

## 📈 测试质量标准

### 覆盖率要求
- **总覆盖率**: ≥ 90%
- **单元测试**: ≥ 85%
- **集成测试**: ≥ 80%
- **关键模块**: ≥ 95%

### 测试编写标准
1. **命名规范**: `test_功能描述()`
2. **文档字符串**: 每个测试函数都有描述
3. **断言清晰**: 使用有意义的断言消息
4. **测试独立**: 每个测试独立运行
5. **Mock 使用**: 合理使用 Mock 隔离外部依赖

### 代码示例
```python
def test_send_email_success(mock_email_settings):
    """测试成功发送邮件"""
    # Arrange
    email_service = EmailService()
    message = EmailMessage(
        to=["recipient@example.com"],
        subject="Test Subject",
        body="Test Body"
    )

    # Act
    with patch.object(email_service, '_connection') as mock_conn:
        mock_conn.sendmail.return_value = {}
        result = email_service.send_email(message)

    # Assert
    assert result == "sent"
    mock_conn.sendmail.assert_called_once()
```

## 🚨 测试风险和缓解策略

### 风险识别
1. **SMTP 服务依赖**: 使用 Mock SMTP 服务器
2. **网络请求**: Mock requests 库
3. **文件系统**: 使用临时文件
4. **环境变量**: 测试环境隔离

### 缓解措施
```python
# 环境隔离
@pytest.fixture(autouse=True)
def env_setup(monkeypatch):
    monkeypatch.setenv("EMAIL_ADDRESS", "test@example.com")
    monkeypatch.setenv("EMAIL_PASSWORD", "test_password")

# 网络Mock
@pytest.fixture
def mock_requests():
    with patch('requests.get') as mock_get:
        mock_get.return_value.content = b"test content"
        yield mock_get
```

## 📊 测试报告和监控

### 覆盖率报告
- **HTML报告**: `pytest --cov-report=html`
- **终端报告**: `pytest --cov-report=term-missing`
- **XML报告**: `pytest --cov-report=xml`

### 性能报告
- **响应时间记录**: 使用 `time.perf_counter()`
- **内存使用监控**: 使用 `memory_profiler`
- **吞吐量测试**: 压力测试数据

### CI/CD 集成
```yaml
# .github/workflows/test.yml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v4
      with:
        python-version: "3.14"
    - name: Install dependencies
      run: |
        pip install uv
        uv sync --dev
    - name: Run tests
      run: |
        uv run pytest --cov=src/email_mcp_server --cov-report=xml
    - name: Upload coverage
      uses: codecov/codecov-action@v3
```

## ✅ 成功标准

### 发布标准（v0.2.0）
- [x] 所有测试通过
- [x] 测试覆盖率 ≥ 90%
- [x] 性能基准达标
- [x] 集成测试通过
- [x] 代码质量检查通过
- [x] 安全扫描通过

### 持续改进
- 定期更新测试用例
- 监控测试覆盖率变化
- 性能基准定期重新评估
- 测试报告分析和改进

---

*创建时间：2025年11月23日*
*目标完成时间：2025年12月7日*
*预计测试覆盖率：90%+*