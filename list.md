目标：基于python实现一个发送邮件的mcp服务器

技术选型：
- 使用uv并基于3.14版本的python创建虚拟环境来管理项目
- 使用Pydantic，FastMCP作为主要框架
- 基于Git来管理版本变更

技术规范：
- 代码和文档均使用UTF-8 not with bom格式
- 代码要有完善的类型注解，并能够通过lint检查
- mcp服务器以stdio方式启动，并提供一个在windows环境下启用虚拟环境项目的bat脚本

核心功能：
- 发件邮箱支持使用qq邮箱和gmail邮箱。根据提供的后缀来判断是哪种类型，内置两种邮箱的标准smtp服务器地址和端口。
- 通过环境变量读取邮箱地址和授权码，测试时读取.env文件的配置。
- 支持传入多个收件邮箱
- 支持传入附件
  - 本地文件：必须使用绝对路径
  - 远程文件：支持url链接，使用系统代理通过get方式下载，可重试3次
- 邮件内容整理完毕后进行启发式问答，当mcp客户端同意后再发送邮件
  - 默认不需要同意，当客户端支持启发式问答时需要用户同意
