# 虚拟环境使用指南

本项目支持两种虚拟环境管理方式：`uv`（推荐）和传统的 `venv`。

## uv 方式（推荐）

### 安装 uv

```bash
# Windows (PowerShell)
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# Linux/macOS
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### 基本命令

```bash
# 创建/激活/同步项目（会自动创建虚拟环境）
uv sync

# 运行命令
uv run python -m email_mcp_server
uv run ruff check src/
uv run pytest

# 添加新依赖
uv add requests

# 添加开发依赖
uv add --dev pytest

# 安装特定版本
uv add "requests>=2.31.0,<3.0.0"

# 更新依赖
uv sync --upgrade

# 清理缓存
uv cache clean
```

### 优势

- [ROCKET] 更快的依赖解析和安装
- [LOCK] 锁定文件确保环境一致性
- 包 自动管理虚拟环境
- [WRENCH] 内置项目管理工具
-  支持多种 Python 版本

## 传统 venv 方式

### 创建虚拟环境

```bash
# Python 3.14+
python -m venv .venv

# 激活虚拟环境
# Windows
.venv\Scripts\activate
# Linux/macOS
source .venv/bin/activate
```

### 基本命令

```bash
# 安装依赖
pip install -e ".[dev]"

# 运行命令
python -m email_mcp_server
ruff check src/
pytest

# 添加新依赖
pip install requests

# 升级依赖
pip install --upgrade requests

# 生成依赖文件
pip freeze > requirements.txt

# 安装依赖文件
pip install -r requirements.txt
```

## 环境变量配置

两种方式都需要相同的环境变量配置：

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件
EMAIL_ADDRESS=your_email@example.com
EMAIL_PASSWORD=your_password_or_auth_code
```

## 启动项目

### 使用启动脚本

**Windows:**
```bash
start_server.bat
```

**Linux/macOS:**
```bash
./start_server.sh
```

### 手动启动

**uv 方式:**
```bash
uv run python -m email_mcp_server
```

**传统方式:**
```bash
# 先激活虚拟环境
# Windows
.venv\Scripts\activate
# Linux/macOS
source .venv/bin/activate

# 然后运行
python -m email_mcp_server
```

## 开发工具

### 代码质量检查

**uv 方式:**
```bash
uv run ruff check src/
uv run ruff check --fix src/
uv run ruff format src/
```

**传统方式:**
```bash
ruff check src/
ruff check --fix src/
ruff format src/
```

### 测试

**uv 方式:**
```bash
# 运行所有测试（185个测试用例，87.22%覆盖率）
uv run pytest

# 运行测试并生成覆盖率报告
uv run pytest --cov=email_mcp_server --cov-report=term-missing

# 运行特定模块测试
uv run pytest tests/test_email_service.py      # 邮件服务测试（97%覆盖率）
uv run pytest tests/test_attachment_service.py # 附件服务测试（84%覆盖率）
uv run pytest tests/test_config.py             # 配置管理测试（89%覆盖率）
uv run pytest tests/test_models.py             # 数据模型测试（92%覆盖率）

# 生成HTML覆盖率报告
uv run pytest --cov=email_mcp_server --cov-report=html
```

**传统方式:**
```bash
# 先新增活虚拟环境，然后运行测试
pytest
pytest --cov=email_mcp_server --cov-report=term-missing
```

### 类型检查

**uv 方式:**
```bash
uv run mypy src/
```

**传统方式:**
```bash
mypy src/
```

## 切换环境

如果需要从 venv 切换到 uv：

1. 删除现有虚拟环境
```bash
# Windows
rmdir /s .venv

# Linux/macOS
rm -rf .venv
```

2. 使用 uv 重新创建
```bash
uv sync
```

3. 更新启动脚本（脚本已支持自动检测）

## 故障排除

### 常见问题

1. **uv 命令未找到**
   - 确保已正确安装 uv
   - 检查 PATH 环境变量

2. **虚拟环境激活失败**
   - 确保使用正确的激活脚本
   - 检查 Python 版本兼容性

3. **依赖安装失败**
   - 检查 Python 版本是否满足要求
   - 尝试升级 pip/uv
   - 检查网络连接

4. **启动脚本权限问题**
   - Linux/macOS: 确保脚本有执行权限 `chmod +x start_server.sh`

### 查看环境信息

**uv 方式:**
```bash
uv info
uv python list
```

**传统方式:**
```bash
python --version
pip list
```

## 最佳实践

1. **推荐使用 uv**：速度更快，功能更强大
2. **保持依赖同步**：定期运行 `uv sync` 或 `pip install -r requirements.txt`
3. **使用开发工具**：利用 ruff、pytest 等工具保证代码质量
4. **版本控制**：将 `uv.lock` 或 `requirements.txt` 提交到版本控制
5. **环境隔离**：每个项目使用独立的虚拟环境